 to run a project       : npx playwright test --project=chromium / firefox
 to run in headed       : npx playwright test --project=chromium --headed
 to show report         : npx playwright show-report
 to debug               : npx playwright test --project=chromium --headed --debug
 to run specific file   : npx playwright test DemoBlaze/Spec/loginTest.spec.ts --project=chromium --headed
 
 to record & 
 write scripts          : npx playwright codegen
 to store               : npx playwright codegen -o tests/myTest.spec.ts
 particular Lang        : npx playwright codegen -o tests/myTest.spec.ts --target=javascript
 chrome                 : npx playwright codegen -o tests/myTest.spec.ts --browser=chromium

                        : npx playwright codegen -o tests/codegenScript.spec.ts --target=javascript  --browser=chromium

  tags                 :  npx playwright test RedBus/Spec/tags.spec.ts --grep "@sanity"  [by using grep only we can run and  @sanity is a tag name , we can give any name]
                       :  npx playwright test RedBus/Spec/tags.spec.ts --grep "@sanity" --grep-invert "@reg"[ to eliminate tag we use invert]

git commands           : create git repo and then in vs code

                            git init
                            git add remote origin https://github.com/Venkat1619/PlaywrightDec2023.git  [repo Url] 
                            git checkout -b <branchName>
                            git status
                            git add. [ add all files]
                            git add fileName[specific file]
                            git commit -m "firstCommit";
                            git push  
                                        
                      :   await page.goto('www.flipcart.com')
screenshot            :   await page.screenshot({path:'Homepage.png'})  // it will store  default 
                      :   await page.screenshot({path:/tests/screenshot'+ 'Homepage.png'})  // stroing it in particular folder
                      :   await page.screenshot({path:'Homepage.png',fullPage:true}) //for fullPage
                      :    await page.locator('particular element locator').screenshot({path:'Homepage.png'}) //for element

group testing         : test.describe()
hooks                 : beforeAll , afterAll ,beforeEach ,afterEach